
const tools = [
  { title: "Language", desc: "沉浸式语言学习与训练", href: "/apps/language" },
  { title: "Qwerty", desc: "单词敲击练习（后续可迁移）", href: "/apps/qwerty" },
  { title: "Calculator", desc: "高频计算与单位换算", href: "/apps/calculator" },
  { title: "Timer", desc: "番茄钟 / 计时 / 提醒", href: "/apps/timer" },
  { title: "Notes", desc: "随手记 & 模板", href: "/apps/notes" },
  { title: "Utilities", desc: "小工具集合入口", href: "/apps/utils" },
];

export default function Home() {
  return (
    <main className="homeRoot">
      {/* Top bar */}
      <header className="homeTop">
        <div className="homeBrand" aria-label="openthedoor brand">
          <LogoMark size={30} />
          <div className="homeBrandText">OPENTHEDOOR</div>
        </div>

        <div className="homeTopRight">
          <ThemeToggle />
        </div>
      </header>

      {/* Hero */}
      <section className="homeHero">
        <div className="homeHeroInner">
          <h1 className="homeTitle">尽有，将有</h1>
          <p className="homeSlogan">ALL THAT IS. ALL TO COME.</p>

          <div className="homeSearch">
            <div className="homeSearchInner">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-hidden>
                <circle cx="11" cy="11" r="7" stroke="currentColor" strokeWidth="1.6" opacity="0.55" />
                <path d="M20 20L17 17" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" opacity="0.55" />
              </svg>
              <input className="homeSearchInput" placeholder="Search tools" aria-label="Search tools" />
              <span className="homeKbd" aria-hidden>⌘ K</span>
            </div>
          </div>

          <div className="homeMeta">
            把常用数字工具与专用模块收进一个入口页。点击卡片打开工具。
          </div>

          <div className="homeCTA">
            <a className="homeBtnPrimary" href="#tools">Explore tools</a>
            <a className="homeBtnGhost" href="#about">About</a>
          </div>
        </div>
      </section>

      {/* Tools */}
      <section className="homeSection" id="tools">
        <div className="homeSectionHead">
          <div className="homeSectionTitle">Modules</div>
          <div className="homeSectionDesc">入口先做得高级、清晰、可扩展。</div>
        </div>

        <div className="homeGrid">
          {tools.map((t) => (
            <a key={t.title} className="homeCard" href={t.href}>
              <div className="homeCardTop">
                <div className="homeCardTitle">{t.title}</div>
                <div className="homePill">OPEN</div>
              </div>
              <div className="homeCardDesc">{t.desc}</div>

              <div className="homeCardPreview" aria-hidden>
                <div className="homePreviewBar" />
                <div className="homePreviewRow">
                  <div className="homeDot" />
                  <div className="homeLine w60" />
                </div>
                <div className="homePreviewRow">
                  <div className="homeDot" />
                  <div className="homeLine w42" />
                </div>
                <div className="homePreviewRow">
                  <div className="homeDot" />
                  <div className="homeLine w76" />
                </div>
              </div>

              <div className="homeCardFoot">
                <span className="homeHint">Click to open</span>
                <span className="homeArrow" aria-hidden>→</span>
              </div>
            </a>
          ))}
        </div>
      </section>

      {/* About */}
      <section className="homeSection" id="about">
        <div className="homeAbout">
          <div className="homeAboutTitle">openthedoor</div>
          <div className="homeAboutDesc">
            这是一个总平台。每个专用模块（例如 Language）都可以拥有自己的界面与体验，但入口统一、干净、可信。
          </div>
        </div>
      </section>

      <footer className="homeFooter">
        <span>© {new Date().getFullYear()} OPENTHEDOOR</span>
        <span className="homeFooterDot">·</span>
        <span className="homeFooterMuted">Built for focus</span>
      </footer>
    </main>
  );
}

function LogoMark({ size = 30 }: { size?: number }) {
  return (
    <svg width={size} height={size} viewBox="0 0 120 120" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
      {/* Linear-ish: crisp geometry + subtle accent */}
      <rect x="12" y="12" width="96" height="96" rx="22" stroke="currentColor" strokeWidth="6" opacity="0.92" />
      <rect x="34" y="28" width="28" height="64" rx="8" stroke="currentColor" strokeWidth="6" opacity="0.92" />
      <path d="M74 30V90" stroke="currentColor" strokeWidth="6" strokeLinecap="round" opacity="0.28" />
      <circle cx="54" cy="60" r="4.5" fill="currentColor" opacity="0.85" />
    </svg>
  );
}
